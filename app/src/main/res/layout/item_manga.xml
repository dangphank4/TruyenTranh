<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/line_back">

    <!-- Outer RelativeLayout for background color -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Inner RelativeLayout with a background image and text -->
        <RelativeLayout

            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:padding="4dp">
            <!-- ImageView for the story's image -->
            <ImageView
                android:id="@+id/imgAnhTruyen"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop" />

            <!-- TextView for new chapter info, at the bottom -->
            <TextView
                android:id="@+id/txvTenChap"
                android:layout_width="match_parent"
                android:layout_height="30dp"
                android:layout_alignParentBottom="true"
                android:background="#A8000000"
                android:gravity="center"
                android:textColor="#ffffff"
                android:textSize="10sp" />
        </RelativeLayout>
    </RelativeLayout>

    <!-- EditText for story name, non-editable -->
    <EditText
        android:id="@+id/txvTenTruyen"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:layout_marginLeft="4dp"
        android:layout_marginTop="8dp"
        android:layout_marginRight="4dp"
        android:layout_marginBottom="8dp"
        android:background="#555555"
        android:gravity="center"
        android:textColor="#ffffff"
        android:textSize="15sp" />
</androidx.appcompat.widget.LinearLayoutCompat>
